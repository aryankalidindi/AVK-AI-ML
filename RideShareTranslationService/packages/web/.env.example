# Web app. Copy to .env.local to override defaults.

# WebSocket relay URL (default ws://localhost:8787).
# VITE_RELAY_URL=ws://localhost:8787

# HTTP base URL for the /translate endpoint (default http://localhost:8787).
# VITE_API_URL=http://localhost:8787
